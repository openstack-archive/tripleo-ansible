---
# Copyright 2021 Red Hat, Inc.
# All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License"); you may
# not use this file except in compliance with the License. You may obtain
# a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.

- name: Ensure tripleo_cephadm_ceph_cli is set
  include_tasks: ceph_cli.yaml
  when:
    - tripleo_cephadm_ceph_cli is not defined

- name: set cephadm debug level
  command: |
    {{ tripleo_cephadm_ceph_cli }} config set mgr mgr/cephadm/log_to_cluster_level debug
  changed_when: false
  become: true
  when: tripleo_cephadm_debug | bool

- name: ceph config set public/private network and ms_bind ipv4/v6
  include_tasks: network_config_set.yaml
  when:
    - tripleo_cephadm_deployed_ceph | default(false) | bool

- name: set default container image in ceph configuration
  command: |
    {{ tripleo_cephadm_ceph_cli }} config set global container_image \
    {{ tripleo_cephadm_container_ns }}/{{ tripleo_cephadm_container_image }}:{{ tripleo_cephadm_container_tag }}
  changed_when: false
  become: true
  when:
    - not (tripleo_cephadm_default_container | default(false) | bool)

- name: enforce minimum ceph clients version
  command: |
    {{ tripleo_cephadm_ceph_cli }} osd set-require-min-compat-client \
    {{ tripleo_cephadm_min_compat_client }}
  changed_when: false
  become: true
  when:
    - tripleo_cephadm_min_compat_client is defined
    - tripleo_cephadm_min_compat_client | length > 0

- name: set container image base in ceph configuration
  command: |
    {{ tripleo_cephadm_ceph_cli }} config set mgr mgr/cephadm/container_image_base \
    {{ tripleo_cephadm_container_ns }}/{{ tripleo_cephadm_container_image }}
  changed_when: false
  become: true
  when:
    - not (tripleo_cephadm_default_container | default(false) | bool)

- name: set dashboard container image in ceph mgr configuration
  when:
    - tripleo_cephadm_dashboard_enabled | bool
    - not (tripleo_cephadm_default_container | default(false) | bool)
  become: true
  block:
    - name: set alertmanager container image in ceph configuration
      command: |
        {{ tripleo_cephadm_ceph_cli }} config set mgr mgr/cephadm/container_image_alertmanager \
        {{ tripleo_cephadm_alertmanager_container_image }}
      changed_when: false

    - name: set grafana container image in ceph configuration
      command: |
        {{ tripleo_cephadm_ceph_cli }} config set mgr mgr/cephadm/container_image_grafana \
        {{ tripleo_cephadm_grafana_container_image }}
      changed_when: false

    - name: set node-exporter container image in ceph configuration
      command: |
        {{ tripleo_cephadm_ceph_cli }} config set mgr mgr/cephadm/container_image_node_exporter \
        {{ tripleo_cephadm_node_exporter_container_image }}
      changed_when: false

    - name: set prometheus container image in ceph configuration
      command: |
        {{ tripleo_cephadm_ceph_cli }} config set mgr mgr/cephadm/container_image_prometheus \
        {{ tripleo_cephadm_prometheus_container_image }}
      changed_when: false

- name: set ingress container images in ceph mgr configuration
  when:
    - tripleo_cephadm_haproxy_container_image is defined
    - tripleo_cephadm_keepalived_container_image is defined
    - not (tripleo_cephadm_default_container | default(false) | bool)
  become: true
  block:
    - name: set haproxy container image in ceph configuration
      command: |
        {{ tripleo_cephadm_ceph_cli }} config set mgr mgr/cephadm/container_image_haproxy \
        {{ tripleo_cephadm_haproxy_container_image }}
      changed_when: false

    - name: set keepalived container image in ceph configuration
      command: |
        {{ tripleo_cephadm_ceph_cli }} config set mgr mgr/cephadm/container_image_keepalived \
        {{ tripleo_cephadm_keepalived_container_image }}
      changed_when: false
